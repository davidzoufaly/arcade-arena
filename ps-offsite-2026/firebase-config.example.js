// Copy this file to `firebase-config.js` and paste your Firebase Web app
// config values. `firebase-config.js` is gitignored.
//
// Get these from Firebase Console -> Project Settings -> Your apps -> Web app.
// `databaseURL` may need to be copied from the Realtime Database tab.

export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  databaseURL: "https://REPLACE_ME.firebaseio.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
