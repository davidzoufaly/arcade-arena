{
  "name": "ps-offsite-games",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "deploy": "npm run build && npx --yes netlify-cli deploy --prod --dir=dist",
    "deploy:preview": "npm run build && npx --yes netlify-cli deploy --dir=dist"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
