{
  "name": "ps-offsite-games",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "vision:assets": "node scripts/fetch-vision-assets.mjs",
    "postinstall": "node scripts/fetch-vision-assets.mjs",
    "predev": "node scripts/fetch-vision-assets.mjs",
    "prebuild": "node scripts/fetch-vision-assets.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14"
  }
}
